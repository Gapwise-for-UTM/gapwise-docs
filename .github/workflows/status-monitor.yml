name: Hourly status monitor

on:
  push:
    branches: [main]
  schedule:
    # Avoid the top of the hour, when GitHub schedules are more likely to queue.
    - cron: "17 * * * *"
  workflow_dispatch:

concurrency:
  # The hourly and operator workflows both update issue #17. Serialize them so
  # a manual incident update cannot be overwritten by a simultaneous probe run.
  group: gapwise-status-publisher
  cancel-in-progress: false

permissions:
  contents: read
  issues: write

jobs:
  status:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "22.13.1"
      - name: Probe production services and publish status
        run: node scripts/update-status.mjs
        env:
          GH_TOKEN: ${{ github.token }}
          STATUS_ISSUE_NUMBER: "17"
